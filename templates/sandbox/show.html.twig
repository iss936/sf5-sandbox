{% extends 'base.html.twig' %}

{% block title %}Sandbox{% endblock %}

{% block body %}
    <h1>Sandbox</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sandbox.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ sandbox.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('sandbox_index') }}">back to list</a>

    <a href="{{ path('sandbox_edit', {'id': sandbox.id}) }}">edit</a>

    {{ include('sandbox/_delete_form.html.twig') }}
{% endblock %}
